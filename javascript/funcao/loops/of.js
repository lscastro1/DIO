// Loops entre estruturas interaveis (Arrays e strings)

let paralvra = 'maçã', array = [1, 2, 3, 'maçã'];

for (let valor of paralvra) {
    console.log(valor);
}

console.log('\n\n');

for (let value of array) {
    console.log(value);
}